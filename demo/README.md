# Demo data — SYNTHETIC, not real deals

`pipeline.json` contains fabricated example deals (listing IDs `NJ-2025-*`,
analysis dates that predate the project). It exists only as a UI/demo fixture.
Do NOT treat these verdicts or numbers as real underwriting output — real
artifacts live under `incoming/` and use bare numeric LoopNet listing IDs.
